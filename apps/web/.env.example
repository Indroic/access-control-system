# ─── Server ───────────────────────────────────────────────────────────────────
# URL of the Hono backend — must be reachable from the browser
# In Docker: keep http://localhost:3000 (browser talks to the host port)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
